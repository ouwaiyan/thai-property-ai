# 泰国房产中介 AI 房源系统 — 项目开发完整度分析报告

> 基准文档：说明书.docx V1.0  
> 分析日期：2026-05-15  
> 最后更新：2026-05-16（第四轮完善 — API 配置管理增强）  
> 用途：后期逐项核对、补齐跟踪

---

## 整体完成度评估

| 维度 | 完成度 | 状态 |
|------|--------|------|
| **核心架构** | ~98% | 完整 |
| **Stage 1: 基础后台** | ~98% | 完整 |
| **Stage 2: 批量导入** | ~95% | 完整 |
| **Stage 3: 地理搜索** | ~98% | 完整 |
| **Stage 4: 路线精算** | ~95% | 完整 |
| **Stage 5: AI 模块** | ~90% | 基本完整 |
| **Stage 6: 推荐闭环** | ~97% | 完整 |
| **Stage 7: LINE/n8n** | ~92% | 基本完整 |
| **总体完成度** | **~97%** | 可上线生产 |

---

## 一、已完整实现 (P0 核心)

### 用户与权限
- [x] JWT 认证（access token + refresh token）
- [x] 四角色权限：Admin / Manager / Agent / Viewer
- [x] audit_logs 审计日志（谁、何时、做了什么、修改前后值）
- [x] 路由级权限控制（deps.py）

### 房源 CRUD
- [x] 完整字段：property_code, name, building_name, address, latitude/longitude, location_geo(PostGIS), district, area, nearest_bts, nearest_mrt, bedroom_count, bathroom_count, size_sqm, monthly_rent, deposit_months, status, available_date, pet_allowed, contact_person, contact_line, contact_phone, description, internal_note, tags
- [x] 状态管理：available / pending / rented / offline / need_confirm
- [x] 列表搜索筛选排序
- [x] 软删除
- [x] 图片管理（封面标记）
- [x] 批量状态/标签/负责人修改（API + 前端 UI）
- [x] CSV 导出（BOM UTF-8，Excel 兼容）

### 批量导入
- [x] 三步流程：上传 → 字段映射与预览 → 确认导入
- [x] CSV / XLSX 支持
- [x] 中英文列名自动识别（60+ 映射项）
- [x] 预览校验（必填/格式/GPS/重复）
- [x] 价格清洗：`฿35,000 / month` → 35000
- [x] GPS 泰国经纬度范围校验
- [x] 错误记录写入 import_errors，可下载错误报告
- [x] 导入后自动触发 geocoding 回填（无 GPS 但有地址的房源）
- [x] 按 property_code 覆盖更新模式

### AI 模块
- [x] 客户需求解析（OpenAI Structured Outputs + JSON Schema）
- [x] 房源标签与亮点生成（仅使用提供事实，不编造）
- [x] 推荐话术生成（zh/en/th 三语、友好/专业/紧迫三语调）
- [x] 数据清洗辅助
- [x] 安全规则：不编造距离/价格/设施/宠物政策

### PostGIS 地理搜索
- [x] PostGIS 扩展 + geography(Point, 4326)
- [x] 空间 GiST 索引
- [x] ST_DWithin 半径查询
- [x] ST_Distance 距离排序
- [x] 组合索引

### 地理编码
- [x] **Google Geocoding API**（优先）+ Nominatim 回退
- [x] **geocode_cache 表持久化**（L1 内存 → L2 数据库 → L3 API 三级缓存）
- [x] **Worker 后台 geocoding 回填**（每5分钟扫描无GPS房源，自动回填）
- [x] **Admin 手动触发 geocoding 回填**（POST /api/v1/geo/backfill）
- [x] **导入后自动触发回填**（确认导入后 geocode 最多 20 条）

### Google Routes API 路线精算
- [x] Compute Route Matrix 真实路线距离/时间
- [x] **三级缓存：Redis → DB → API**
- [x] Haversine 回退
- [x] 日调用限量 + 候选数量限制

### 匹配评分算法
- [x] match_score = distance×0.40 + budget×0.25 + bedroom×0.20 + tag×0.10 + freshness×0.05

### LINE 集成
- [x] Webhook 签名验证 + 消息收发
- [x] AI 回复建议 + 自动回复开关
- [x] Rich Menu CRUD
- [x] Rich Menu 图片上传（前端集成）
- [x] LINE 审核页面 + LINE 设置页面

### n8n 自动化
- [x] Webhook 通知（新客户 / 导入完成 / 状态变更）
- [x] 每日统计 / 过期房源 / 未跟进客户端点

### API 配置中心
- [x] 密钥加密存储（encrypted_value）
- [x] **测试连接按钮**（OpenAI / Google Maps / LINE）
- [x] **前端 API 配置管理页面**（/dashboard/api-settings）
- [x] 启停切换

### 前端页面
- [x] 登录 / 仪表盘
- [x] 房源列表（搜索/筛选/排序/**批量选择+操作**/**导出CSV**）
- [x] 房源详情/编辑/新建（**含街景按钮**）
- [x] 批量导入（上传/映射/预览/确认/历史/错误详情）
- [x] 客户列表/详情（含 AI 解析、推荐搜索）
- [x] **推荐结果独立页面**（/dashboard/recommendations — 选择客户 → 搜索 → 生成话术 → 标记发送 + 历史记录 + **地图视图**）
- [x] AI 工具箱（需求解析/标签生成/话术生成/数据清洗）
- [x] LINE 审核 / LINE 设置（**含 Rich Menu 图片上传**）
- [x] **API 配置中心**（/dashboard/api-settings — 密钥管理 + 测试连接）
- [x] 报表（房源统计/客户漏斗/推荐统计）
- [x] 用户管理 + 操作日志

### i18n 国际化
- [x] 中文 / English / ไทย 三语
- [x] 前端 + 后端 API 错误信息全覆盖
- [x] 新增页面（API配置、推荐结果、批量操作、导出）翻译已补齐

### 部署与安全
- [x] Docker Compose（Nginx + Frontend + Backend + PostgreSQL/PostGIS + Redis + n8n）
- [x] deploy.sh 一键部署 + backup.sh 备份
- [x] HTTPS / 安全头 / CORS
- [x] 启动安全检查
- [x] Seed 脚本

### Redis 缓存
- [x] Redis 客户端（graceful degradation — 不可用时自动降级）
- [x] 路由矩阵结果缓存到 Redis
- [x] 地理编码结果缓存到 Redis

---

## 二、第二轮新增实现（2026-05-15 上午）

### 高优先级完善项

| # | 项目 | 变更文件 | 说明 |
|---|------|---------|------|
| 1 | **Google Geocoding API** | `app/utils/geo.py` | 新增 `geocode_google()`，`geocode()` 优先 Google，回退 Nominatim |
| 2 | **geocode_cache 持久化** | `app/models/geocode_cache.py`, `geo.py`, 迁移文件 | L1内存→L2数据库→L3 API 三级缓存 |
| 3 | **Worker 后台任务** | `app/worker.py`, `docker-compose.yml` | 每5分钟扫描无GPS房源并回填坐标 |
| 4 | **房源导出 CSV** | `app/api/v1/properties.py`, 前端 `page.tsx` | 后端导出端点 + 前端导出按钮 |
| 5 | **批量操作 UI** | 前端 `properties/page.tsx` | 行选择 + 批量修改状态/标签工具栏 |
| 6 | **Redis 缓存接入** | `app/utils/redis_client.py`, `routing_service.py` | 路由矩阵 Redis→DB→API 三级缓存 |
| 7 | **API 测试连接** | `app/services/settings_service.py`, `api_settings.py` | OpenAI/Google Maps/LINE 连通性测试 |
| 8 | **导入触发 geocoding** | `app/services/import_service.py` | 导入确认后自动回填 GPS |
| 9 | **Admin 手动回填** | `app/api/v1/geo.py` | `POST /geo/backfill` 管理员触发 |
| 10 | **推荐结果独立页** | `frontend/.../recommendations/page.tsx` | 独立推荐页：选客户→搜索→话术→发送+历史 |
| 11 | **API 配置前端页** | `frontend/.../api-settings/page.tsx` | 密钥管理 + 测试连接按钮 |
| 12 | **i18n 补齐** | `zh.json` / `en.json` / `th.json` | 新增 settings + recommendations 命名空间 |

### 本轮涉及文件

**后端 (10 文件)**
- `app/config.py` — 新增 REDIS_URL
- `app/utils/geo.py` — 重写：Google Geocoding + DB 缓存 + 三级缓存
- `app/utils/redis_client.py` — 新建 Redis 客户端
- `app/models/geocode_cache.py` — 新建模型
- `app/models/__init__.py` — 注册 GeocodeCache
- `app/worker.py` — 新建后台 Worker
- `app/services/settings_service.py` — 新增 test_connection
- `app/services/routing_service.py` — Redis 缓存接入
- `app/services/property_service.py` — 新增 bulk_update
- `app/services/import_service.py` — 导入后触发 geocoding 回填
- `app/services/recommendation_service.py` — geocode 传 db
- `app/api/v1/properties.py` — 新增 export/bulk-update 端点
- `app/api/v1/geo.py` — geocode 传 db + provider + /backfill 端点
- `app/api/v1/api_settings.py` — 新增 test-connection 端点
- `alembic/versions/a1b2c3d4e5f6_*.py` — geocode_cache 迁移
- `requirements.txt` — 新增 redis 依赖
- `docker-compose.yml` — worker 命令更新

**前端 (8 文件)**
- `lib/api.ts` — 新增 export/bulkUpdate/testConnection/getApiSettings/triggerBackfill API
- `app/dashboard/properties/page.tsx` — 批量选择 + 导出按钮
- `app/dashboard/recommendations/page.tsx` — 新建推荐结果独立页面
- `app/dashboard/api-settings/page.tsx` — 新建 API 配置中心页面
- `components/ProLayoutWrapper.tsx` — 新增推荐结果 + API配置菜单入口
- `i18n/zh.json` — 新增 30+ 翻译键
- `i18n/en.json` — 新增 30+ 翻译键
- `i18n/th.json` — 新增 30+ 翻译键

---

## 三、第三轮新增实现（2026-05-15 下午 — 中等优先级全部完成）

| # | 项目 | 变更文件 | 说明 |
|---|------|---------|------|
| 1 | **S3/R2 对象存储** | `app/services/storage_service.py` (新), `app/config.py`, `property_image_service.py`, `requirements.txt` | S3 兼容存储抽象层（boto3），自动检测 S3 可用性，不可用时回退本地存储 |
| 2 | **字段级权限控制** | `app/utils/field_permission.py` (新), `api/v1/properties.py`, `api/v1/leads.py` | 按角色字段级脱敏：Admin/Manager 全量，Agent 部分掩码（0812345678→081****678），Viewer 全部 null |
| 3 | **LINE Rich Menu 图片上传** | `app/services/line_service.py`, `api/v1/line_rich_menu.py`, 前端 `lib/api.ts`, `line-settings/page.tsx`, i18n | 创建 Rich Menu 时可上传 JPEG/PNG 图片（≤1MB），前端 Upload 组件 + 后端 LINE API 代理 |
| 4 | **Google Maps 前端集成** | 前端 `lib/googleMaps.ts` (新), `components/StreetViewModal.tsx` (新), `components/PropertyMap.tsx`, `app/dashboard/properties/[id]/page.tsx`, `app/dashboard/recommendations/page.tsx`, `app/dashboard/page.tsx`, i18n, `package.json` | Google Maps JS SDK 加载器（后端密钥自动获取 + env fallback）、Street View 街景弹窗、PropertyMap 增强（标记聚类+街景/导航按钮）、推荐页新增地图视图、后端推荐搜索 API 返回经纬度 |

### 本轮涉及文件

**后端 (10 文件)**
- `app/services/storage_service.py` — 新建 S3 兼容存储层
- `app/config.py` — 新增 STORAGE_BACKEND/S3_* 配置
- `app/services/property_image_service.py` — 改用 storage_service
- `app/services/recommendation_service.py` — 搜索结果返回 latitude/longitude
- `app/utils/field_permission.py` — 新建字段级权限控制
- `app/api/v1/properties.py` — 应用字段权限
- `app/api/v1/leads.py` — 应用字段权限
- `app/services/line_service.py` — 新增 upload_rich_menu_image
- `app/api/v1/line_rich_menu.py` — 新增图片上传端点
- `requirements.txt` — 新增 boto3

**前端 (10 文件)**
- `lib/api.ts` — 新增 uploadRichMenuImage
- `lib/googleMaps.ts` — 新建 Google Maps 加载器
- `components/StreetViewModal.tsx` — 新建街景弹窗
- `components/PropertyMap.tsx` — 增强：聚类、街景/导航按钮
- `app/dashboard/properties/[id]/page.tsx` — 新增街景按钮
- `app/dashboard/recommendations/page.tsx` — 新增地图视图 Tab
- `app/dashboard/line-settings/page.tsx` — 新增图片上传 UI
- `app/dashboard/page.tsx` — 传递交通信息到地图
- `i18n/zh.json, en.json, th.json` — 新增 street_view/directions/menu_image/image_type_invalid/image_too_large
- `package.json` — 新增 @googlemaps/js-api-loader

---

## 四、第四轮完善（2026-05-16 — API 配置管理增强）

| # | 项目 | 变更文件 | 说明 |
|---|------|---------|------|
| 1 | **后端 toggle/delete 补齐** | `app/services/settings_service.py` | 新增 `toggle_setting()` / `delete_setting()` 函数（API 端点已引用但服务层缺失） |
| 2 | **updated_at 返回** | `settings_service.py`, `api/v1/api_settings.py` | list/upsert/toggle 返回值均包含 `updated_at` 时间戳 |
| 3 | **前端 toggle API** | `frontend/.../lib/api.ts` | 新增 `toggleApiSetting()` 调用 PATCH 端点 |
| 4 | **API 配置页加强** | `frontend/.../api-settings/page.tsx` | 状态列内联 Switch 直接启停、Provider 下拉选择、updated_at 列、编辑时锁定 provider/key_name、i18n 全覆盖、移除冗余快速测试卡片 |
| 5 | **i18n 补齐** | `zh.json / en.json / th.json` | 新增 `settings.updated_at/toggle_failed/toggle_success` + `common.required` 翻译键 |

### 本轮涉及文件

**后端 (2 文件)**
- `app/services/settings_service.py` — 新增 toggle_setting、delete_setting、updated_at 返回
- `app/api/v1/api_settings.py` — ApiSettingOut schema 新增 updated_at

**前端 (5 文件)**
- `lib/api.ts` — 新增 toggleApiSetting
- `app/dashboard/api-settings/page.tsx` — 全面增强（内联开关、Provider Select、时间戳、i18n）
- `i18n/zh.json, en.json, th.json` — 新增 4 个翻译键

---

## 五、剩余缺口（后续迭代 P2）

| # | 缺口 | 详情 |
|---|------|------|
| 1 | WhatsApp / 邮件渠道 | 规格 P2 范围 |
| 2 | 复杂 CRM 报表（转化率趋势、ROI） | 当前有基础统计 |
| 3 | LINE 自动回复默认人工审核 | 自动回复开关已实现，可配置 |
| 4 | MapPicker 升级 Google Maps 瓦片 | 当前使用 OpenStreetMap 瓦片，可切到 Google Maps 瓦片（需 API Key） |

---

## 六、验收标准逐项对照（规格 §19.1）

| # | 验收标准 | 状态 | 备注 |
|---|---------|:--:|------|
| 1 | 客户输入"我想找 Asok 附近两房，预算 35000，可以养猫"→ AI解析地点/预算/户型/宠物 | ✅ | Structured Outputs |
| 2 | Asok BTS 转经纬度，查询 3km 内 available 房源 | ✅ | Google Geocoding(优先) + Nominatim(回退) + PostGIS |
| 3 | 不推荐 rented/offline 房源 | ✅ | `status == 'available'` |
| 4 | 不推荐不可养宠物房源给 pet_required=true 客户 | ✅ | `pet_allowed == True` |
| 5 | 推荐话术只包含数据库字段和地图结果 | ✅ | AI prompt 安全规则 |
| 6 | 批量导入"฿35,000 / month" → 35000 | ✅ | `_clean_price()` |
| 7 | 无经纬度但有地址的房源进入 geocoding 队列并回填 GPS | ✅ | Worker + 导入后触发 + Admin 手动触发 |
| 8 | 普通 Agent 无法查看 API Key 或导出全部客户数据 | ✅ | Admin 级权限 |
| 9 | Google Routes API 失败时保留 PostGIS 距离结果 | ✅ | Haversine 回退 |

---

## 七、技术栈对照

| 层级 | 规格推荐 | 实际实现 |
|------|---------|---------|
| 前端 | Next.js / React Admin / Ant Design Pro | Next.js + Ant Design Pro Components |
| 后端 | FastAPI 或 NestJS | FastAPI |
| 数据库 | PostgreSQL + PostGIS | PostgreSQL 16 + PostGIS 3.5 |
| 缓存 | Redis | Redis 7（已接入 route + geocode 缓存） |
| AI | OpenAI API | OpenAI API（Structured Outputs + JSON Schema） |
| 地图 | Google Maps API | Google Maps Geocoding + Routes API + Nominatim 回退 |
| 对象存储 | Cloudflare R2 / S3 | S3 兼容存储抽象层（boto3），支持 R2/S3/DigitalOcean，本地回退 |
| 自动化 | n8n | n8n（Webhook 通知 + 数据端点） |
| 部署 | Docker + Nginx + Ubuntu | Docker Compose + Nginx + deploy.sh |

---

## 八、数据库表清单

| 表名 | 状态 | 说明 |
|------|:--:|------|
| users | ✅ | 用户与角色 |
| properties | ✅ | 房源（含 location_geo PostGIS） |
| property_images | ✅ | 房源图片 |
| leads | ✅ | 客户线索 |
| recommendations | ✅ | 推荐记录 |
| line_messages | ✅ | LINE 消息 |
| import_jobs | ✅ | 导入任务 |
| import_errors | ✅ | 导入错误 |
| audit_logs | ✅ | 审计日志 |
| api_settings | ✅ | API 密钥配置 |
| route_cache | ✅ | 路线缓存 |
| geocode_cache | ✅ | 地理编码缓存（新增） |

---

## 九、API 端点清单

| 方法 | 路径 | 状态 | 说明 |
|------|------|:--:|------|
| POST | /api/v1/auth/login | ✅ | 登录 |
| POST | /api/v1/auth/logout | ✅ | 退出 |
| GET/POST | /api/v1/users | ✅ | 用户管理 |
| GET/POST/PUT/DELETE | /api/v1/properties/{id} | ✅ | 房源 CRUD |
| GET | /api/v1/properties/export/csv | ✅ | 导出 CSV（新增） |
| POST | /api/v1/properties/bulk-update | ✅ | 批量更新（新增） |
| POST | /api/v1/properties/import | ✅ | 导入上传 |
| GET/POST | /api/v1/imports/{id}/* | ✅ | 导入流程 |
| POST | /api/v1/geo/geocode | ✅ | 地址地理编码 |
| POST | /api/v1/geo/backfill | ✅ | 手动触发回填（新增） |
| POST | /api/v1/transit/route-matrix | ✅ | 路线矩阵 |
| GET/POST/PUT | /api/v1/leads | ✅ | 客户管理 |
| POST | /api/v1/ai/parse-lead | ✅ | AI 需求解析 |
| POST | /api/v1/ai/generate-tags | ✅ | AI 标签生成 |
| POST | /api/v1/ai/generate-message | ✅ | AI 话术生成 |
| POST | /api/v1/ai/clean-data | ✅ | AI 数据清洗 |
| POST | /api/v1/recommendations/search | ✅ | 推荐搜索 |
| GET/POST | /api/v1/recommendations/* | ✅ | 推荐管理 |
| POST | /api/v1/line/webhook | ✅ | LINE Webhook |
| GET/POST | /api/v1/line/conversations | ✅ | LINE 对话 |
| GET/PUT/POST/DELETE | /api/v1/line/settings/* | ✅ | LINE 设置 |
| GET/PUT/PATCH/DELETE | /api/v1/admin/settings/* | ✅ | API 配置 CRUD |
| POST | /api/v1/admin/settings/test-connection/{provider} | ✅ | 测试连接（新增） |
| GET | /api/v1/reports/properties | ✅ | 房源报表 |
| GET | /api/v1/reports/leads | ✅ | 客户报表 |
| GET | /api/v1/reports/recommendations | ✅ | 推荐报表 |
| GET | /api/v1/audit-logs | ✅ | 审计日志 |
| GET | /api/v1/n8n/* | ✅ | n8n 数据端点 |
| GET | /health | ✅ | 健康检查 |
